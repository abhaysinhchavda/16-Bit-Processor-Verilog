Here 

Design folder which is contain all design module of 16 bit processor

Document folder which is contain flow chart and block diagram

Simulation folder which is contain all simulated file In that .vvp file is simulated with help of icarus verilog tool and .vcd file is simulated with gtkwave tool.

Test bench folder which contain individual test bench of each module.